[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"


[tool.pytest]
pythonpath = [
    ".",
    "src"
]
testpaths = ["tests"]
python_files = "test_*.py"
addopts = "--import-mode=importlib"

[project]
name = "orionis"
version = "0.1.0"